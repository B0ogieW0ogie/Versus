<div class="max-w-2xl mx-auto px-4 py-12">
    <h1 class="text-xl font-semibold text-white">{{ __('battle.create_title') }}</h1>
    <p class="mt-3 text-sm text-white/60 leading-relaxed">{{ __('battle.create_placeholder') }}</p>
    <p class="mt-8">
        <a href="{{ route('home') }}"
           class="text-sm text-glow-cyan hover:underline">{{ __('battle.back_to_battles') }}</a>
    </p>
</div>
